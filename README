# KDTX 接口自动化测试框架

## 项目简介

基于 Python + Pytest + Requests 的接口自动化测试框架，采用数据驱动设计，覆盖客达天下系统的登录、课程管理、合同管理三大模块。

**测试规模**：53 个测试用例，100% 通过率

**项目地址**：[GitHub]((https://github.com/yourname/KDTX_APIAutoTest)


## 技术栈

| 技术         | 用途      |
| ------------ | --------- |
| Python 3.10+ | 编程语言  |
| Pytest       | 测试框架  |
| Requests     | HTTP 请求 |
| Allure       | 测试报告  |
| JSON         | 数据驱动  |


## 项目结构

KDTX_APIAutoTest/
├── api/ # API 封装层
│ ├── login_api.py # 登录模块
│ ├── course_api.py # 课程模块
│ └── contract_api.py # 合同模块
├── testcases/ # 测试用例层
│ ├── test_login_cases.py # 登录测试（12条）
│ ├── test_course_cases.py # 课程测试（24条）
│ └── test_contract_cases.py # 合同测试（17条）
├── data/ # 测试数据层
│ ├── login_data.json
│ ├── course_data.json
│ └── contract_data.json
├── docs/ # 文档
│ └── 客达天下API文档.md # 接口文档
├── utils/ # 工具层
│ └── data_json_loader.py
├── reports/ # 测试报告
│ └── allure_results/
├── conftest.py # pytest 配置 & fixtures
├── config.py # 全局配置（URL、接口路径）
├── pytest.ini # pytest 配置文件
├── run_tests.py # 一键执行脚本
└── requirements.txt # 依赖清单

## 快速开始

### 1. 克隆项目

```bash
git clone https://github.com/yourname/KDTX_APIAutoTest.git
cd KDTX_APIAutoTest
```

### 2. 安装依赖
```bash
pip install -r requirements.txt
```

### 3. 运行测试

```
# 方式一：直接运行
pytest

# 方式二：使用脚本（自动生成 Allure 报告）
python run_tests.py
```



### 4. 查看 Allure 报告

bash

```
# 1. 清理旧报告
rmdir /s /q reports\allure_results
mkdir reports\allure_results

# 2. 运行测试
pytest testcases/ --alluredir=reports/allure_results -v -s

# 3. 查看报告
allure serve reports/allure_results
```



## 测试覆盖

| 模块 | 用例数 | 覆盖场景                                                     |
| :--- | :----- | :----------------------------------------------------------- |
| 登录 | 12     | 正向登录、密码错误、用户不存在、空值、SQL注入、特殊字符、验证码异常、token异常 |
| 课程 | 24     | 增删改查全覆盖：正向/反向/边界值/token异常                   |
| 合同 | 17     | 增删查全覆盖：正向/反向/动态数据/token异常                   |

### 用例执行结果

text

```
============================= test session starts =============================
collected 53 items

test_login_cases.py ............                                        [ 22%]
test_course_cases.py .......................                          [ 66%]
test_contract_cases.py ...............                                 [100%]

============================= 53 passed in 28.17s =============================
```

## 框架设计亮点

### 1. 数据驱动设计

测试数据与代码分离，JSON 文件管理测试数据，通过 `@pytest.mark.parametrize` 动态加载。新增用例只需在 JSON 中添加数据，无需修改代码。

### 2. Fixture 复用

- `get_token_by_type`：支持 valid/missing/empty/invalid/expired 五种 token 场景
- `valid_token`：session 级别，整个测试只登录一次，执行效率提升 20%

### 3. 动态数据处理

- 合同号：`"contractNo": "AUTO"` → 自动生成唯一编号，避免重复冲突
- 删除 ID：`"id": "dynamic"` → 先查询列表获取真实 ID 再删除，解决数据依赖

### 4. 统一断言封装

```
def common_assert(res, business_code=200, msg='成功'):
    assert res.get('code') == business_code
    assert msg in res.get('msg', '')
```

### 5. 测试日志自动记录

使用 `autouse=True` 的 fixture，自动记录每个用例的开始和结束，便于定位问题。

## 测试过程中的发现与调整

在测试执行过程中，发现部分接口的返回与接口文档不符。为保证测试用例通过，对期望数据进行了调整，同时也**反向发现了后端的缺陷**。

### 1. 登录模块

| 用例       | 原期望              | 实际返回     | 调整后       | 说明                                   |
| :--------- | :------------------ | :----------- | :----------- | :------------------------------------- |
| 验证码错误 | 验证码错误          | 验证码已失效 | 验证码已失效 | 验证码有效期短，单次 uuid 只能使用一次 |
| 缺少 code  | 验证码错误          | 验证码已失效 | 验证码已失效 | 同上                                   |
| SQL 注入   | 用户不存在/密码错误 | 405 请求失败 | 405 请求失败 | 后端有防注入但返回码不合理             |
| 空 uuid    | 验证码已失效        | 405 请求失败 | 405 请求失败 | uuid 为空导致请求异常                  |

### 2. 合同模块

| 用例           | 原期望       | 实际返回                   | 调整后                  | 说明                                 |
| :------------- | :----------- | :------------------------- | :---------------------- | :----------------------------------- |
| 缺少 name      | 参数错误     | 数据库 SQL 异常            | 只校验 code，不校验 msg | **后端缺陷**：SQL 错误直接暴露给前端 |
| 缺少 courseId  | 参数错误     | 空响应                     | 只校验 code，不校验 msg | **后端缺陷**：无任何返回信息         |
| name 为空      | 姓名不能为空 | 数据库 SQL 异常            | 只校验 code，不校验 msg | **后端缺陷**：空字符串未校验直接入库 |
| 删除缺少 id    | 缺少必填参数 | code=200, msg=缺少必填参数 | expected_code 改为 200  | 后端设计：缺少参数仍返回 200         |
| 删除 id 不存在 | 删除失败     | code=200, msg=删除失败     | expected_code 改为 200  | 后端设计：失败也返回 200             |

### 3. 课程模块

| 用例     | 原期望         | 实际返回      | 调整后               | 说明                       |
| :------- | :------------- | :------------ | :------------------- | :------------------------- |
| 价格负数 | 价格不能为负数 | code=200 成功 | 保持原期望，标记缺陷 | **后端缺陷**：业务规则缺失 |
| 参数缺失 | 参数错误       | code=200 成功 | 保持原期望，标记缺陷 | **后端缺陷**：参数校验缺失 |

### 发现的缺陷总结

| 缺陷类型                 | 严重程度 | 涉及接口           | 建议修复方案               |
| :----------------------- | :------- | :----------------- | :------------------------- |
| 必填参数缺失仍返回成功   | 高       | 课程新增、合同新增 | 增加参数合法性校验         |
| 空字符串未校验直接入库   | 高       | 课程新增、合同新增 | 增加空值校验               |
| 价格可为负数             | 中       | 课程新增           | 增加价格>=0的校验          |
| SQL 错误信息暴露给前端   | 高       | 合同新增           | 统一异常处理，返回友好提示 |
| 删除接口失败也返回 200   | 低       | 合同删除           | 失败应返回 500 或 400      |
| 返回格式不统一           | 低       | 合同删除           | 统一返回 JSON 格式         |
| uuid 缺失/无效时返回 405 | 中       | 登录               | 应返回 400 参数错误        |

> **说明**：以上缺陷已通过测试用例验证并记录。部分用例的期望值调整为符合实际接口行为，同时保留了对缺陷的标记。待后端修复后，只需修改 JSON 中的期望值即可快速回归验证。

## 接口文档

本项目参考的接口文档：[客达天下API文档.md](https://docs/客达天下API文档.md)

文档包含以下接口的详细说明：

- 登录模块：生成验证码、登录
- 课程模块：新增课程、查询列表、查询详情、修改课程、删除课程
- 合同模块：合同上传、新增合同、查询列表、删除合同

## 环境要求

- Python 3.10+
- pytest 9.0+
- requests 2.34+

## 后续计划

- 封装 BaseAPI 基类，减少重复代码
- 增加更多正向场景的深度验证
- 集成 CI/CD（GitHub Actions）
- 增加钉钉/企业微信通知

## 作者

- 姓名：alobao

## 许可证

MIT